version="3.12"
#baseurl="http://downloads.sourceforge.net/mingw/w32api-${version}.tar.gz"
baseurl="http://downloads.sourceforge.net/mingw/w32api-${version}-mingw32-dev.tar.gz"

goto_srcdir()
{
    true
}

install()
{
    cp -rpv include $prefix
    cp -rpv lib $prefix
}
