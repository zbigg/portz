version=1.8.2
baseurl=http://files.randombit.net/botan/Botan-${version}.tbz
web=http://botan.randombit.net/

install() {    
    ./configure.pl --prefix=$prefix --exec-prefix=$exec_prefix --cc=gcc
    
    $MAKE -j$cpus
    $MAKE install DESTDIR=${DESTDIR}$prefix
}

