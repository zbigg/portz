version=1.7.14
baseurl=http://files.randombit.net/botan/Botan-${version}.tbz
web=http://botan.randombit.net/

install() {    
    ./configure.pl --prefix=$prefix --cc=gcc
    
    make -j$cpus
    make install
}
